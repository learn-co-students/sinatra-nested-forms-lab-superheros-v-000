<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Our Team</title>
  </head>
  <body>
    <h1><%=Team.all.first.name%></h1>
    <h2>Team Motto: <%=Team.all.first.motto%></h2>
    <%Hero.all.each{ |hero| %>
      <h2><%="Hero Name: #{hero.name}"%> </h2>
      <h3><%="Hero Power: #{hero.power}"%> </h3>
      <h3><%="Hero Biography: #{hero.bio}"%> </h3>
    <%}%>
  </body>
</html>
